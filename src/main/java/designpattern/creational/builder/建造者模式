定义：将一个复杂对象的构建与它的表示分离，是的同样的构建过程可以创建不同的表示，用户
     只需要指定需要建造的类型就可以得到它们，建造过程及细节不需要知道
类型：创建型
适用场景：如果一个对象有非常复杂的内部结构（很多属性），下岗吧复杂对象的创建和适用分离
优点：封装性好，创建和适用分离，扩展性好建造类之间独立一定程度上解耦
缺点：产生多余的builder对象，产品内部发生变化，见在这都需要修改，成本较大
和工厂模式的区别：建造者模式注重方法调用顺序而工厂模式注重创建产品，创建对象的粒度不同，
                 建造者模式可以创建复杂产品由复杂组件组成但工厂模式创建出来的都是一个
                 样，工厂模式只要创建出产品就可以而建造者模式还要知道这个产品由那些部
                 件组成，调用方法顺序不同会创建出不同产品
源码中的使用：
  JDK：StringBuilder类append方法
       StringBuffer类append方法加上了synchronized保护
       ImmutableSet类build方法add方法
  Guava：cache
       cachebuilder.newbuilder.继续链式调用
  Spring：BeanDefinitionBuilder
  Mybatis：SqlSessionFactoryBuilder